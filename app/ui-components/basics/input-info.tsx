import { styled } from "@pigment-css/react";

const InputInfo = styled("div")({
    width: "100%", display: "flex", flexDirection: "row",
    alignItems: "center", justifyContent: "space-between", marginBottom: "1rem",
})

export default InputInfo;